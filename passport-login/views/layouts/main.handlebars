<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Passport Auth App</title>
        <link rel="stylesheet" href="/public/css/style.css">
    </head>
    <body>
        {{#if errors}}
            {{#each errors}}
                <div class="alert alert-danger">{{msg}}</div>
            {{/each}}
        {{/if}}
        {{{body}}}
    </body>
</html>