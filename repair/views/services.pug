extends layout

block content
    h1 #{title}

    each service, i in services
        div.well
            h3 #{service.name}
            p #{service.description}
            p
                | Price Rage: #{service.price_range}